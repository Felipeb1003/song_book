<h2>This url doesn't exist... </h2><br>

   <%if is_logged_in? %>
    <a href="/users/<%=current_user.id%>"> Home Page</a>
   <%else%>
    <div class=signup>
      <p> Please <a href="/users/signup">Sign Up</a> or <a href="/users/login">Log In</a> to continue.</p>
    </div>
   <%end%>
